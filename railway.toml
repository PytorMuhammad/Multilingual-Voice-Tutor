[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m streamlit run multilingual_voice_tutor_enhanced.py --server.port $PORT --server.address 0.0.0.0 --server.headless true --server.enableCORS false --server.enableXsrfProtection false"

[env]
PYTHONUNBUFFERED = "1"
PORT = "8080"
